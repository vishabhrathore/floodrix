Got it — conflicting signals between Q2 and Q4 but Q4 is your final answer, so:

**Scope for Chunk 4: Infrastructure only.** 6 files. No handlers. Just the Inngest plumbing + RunOrchestrator + SessionPoller. All 5 async handlers stay as "not implemented" stubs.

Reasoning: you answered "Full Inngest-backed subworkflow" in Q2 but then said "do not create subworkflow pdf and other async node, just do Inngest" in Q4. Q4 is the latest and most specific instruction, so I'm following it. SUBWORKFLOW/PDF/API_CALL/LOOP/PARALLEL will come in a future chunk once this infrastructure is verified working.

Before I write, two small checks:I'll pick sensible defaults and start writing so I don't block on these — you can tell me if they're wrong:

- **Strategy: INLINE_ASYNC** (run sync inline, hand off to Inngest when you hit an async node). Matches the mental model and gives the best responsiveness for most workflows.
- **Polling: Adaptive** (1s/2s/5s). Sweet spot between responsiveness and server load. Long-polling adds complexity we don't need yet.

If you want different, say so and I'll adjust — for now I'm committing.

Starting Chunk 4 now.### File 1/8 — types.ts addition (RunStrategy enum + async helpers)### File 2/8 — RunStrategyResolver### File 3/8 — Inngest session-resume function### File 4/8 — Inngest session-start function (for BACKGROUND_BATCH)### File 5/8 — SessionPoller### File 6/8 — RunOrchestrator### File 7/8 — updated execution-router with poll + orchestrator### File 8/8 — updated bootstrap + Inngest route + READMENow the updated Inngest client registration and README:## Chunk 4 delivered — 9 files (Inngest infrastructure)

**What you can do now that you couldn't before:**

- Single entry point (`RunOrchestrator`) that picks the right strategy automatically
- Batch jobs ≥ 25 rows automatically go to background via Inngest
- Workflows with async nodes are recognized and handed off to Inngest (they just error on "not implemented" until handlers land)
- Server-side adaptive polling endpoint with backoff logic
- Type-safe RunStrategy enum that drives both resolver and orchestrator decisions

**What's still missing (by design):**

- The 5 async handlers (API_CALL, PDF_REPORT, SUBWORKFLOW, LOOP, PARALLEL)
- Session TTL cleanup
- Metrics / observability hooks
- True Workers-based handler timeouts

### Apply order (high level)

1. Confirm `inngest` is in `package.json` and `src/inngest/client.ts` exists (you said yes)
2. Replace 3 files (types.ts, bootstrap.ts, execution-router.ts)
3. Add 5 new files (RunStrategyResolver, RunOrchestrator, SessionPoller, 2 Inngest functions)
4. Merge `src/inngest/functions.ts` — don't overwrite, just add two imports + two array entries
5. `npx tsc --noEmit`
6. Start dev, run `wf_dicken` — should behave identically to Chunk 3

The README has the full checklist, merge instructions, and expected behavior for each strategy path.

### A few honest flags about this chunk

**1. `runBackgroundBatch` in the orchestrator is slightly wasteful.** It calls the executor to create the session + run the sync prefix, *then* also emits the Inngest event. The executor's status guards prevent actual double-work, but it's ~1 extra round-trip per batch start. A cleaner refactor would add `repo.createSessionShellOnly()` and skip the executor entirely for this path. I punted on it because it required a new repo method and this chunk is already 9 files. Noted in the README.

**2. The poll endpoint uses straight DB polling, not SSE.** Fine for dozens of concurrent runs; at thousands you'd want server-sent events. Don't optimize until it's a real problem.

**3. Idempotency key lookup has no TTL.** A key from 6 months ago still dedupes. Probably fine for practical use; add a window if it ever matters.

**4. I can't compile-check this without your project open.** There's a nonzero chance of an import path mismatch or a subtle type error I didn't catch. Run `npx tsc --noEmit` first thing after applying and paste any errors — fixes are usually one-liners.

When you're ready for Chunk 4.5 (the actual async handlers — ApiCallHandler via ky, PdfReportHandler, SubworkflowHandler with depth guard, LoopHandler, ParallelHandler), say so and we'll do them. That's when workflows with HTTP calls start actually working end-to-end.

Or if you'd rather jump to Chunk 5 (hardening — TTL, metrics, timeouts) and defer handlers, that works too. Your call.
